name: 'Terraform'

on:
  push:
    branches:
      - main

jobs:
  terraform init:
    name: My first job
    runs-on: self-hosted
    steps:
      - name: terraform init
        run: terraform init 
      - name: terraform validate
        run: terraform validate
      - name: terraform fmt
        run: terraform fmt
        

